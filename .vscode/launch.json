{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "robotframework-lsp",
            "name": "Robot Framework: Launch template",
            "request": "launch",
            "cwd": "${workspaceFolder}/test/robot",
            "args": ["--outputdir", "${workspaceFolder}/test/robot/results"],
            "env": {
                "MSP_HOST_URL": "http://localhost:8081"
            }
        }
    ]
}
